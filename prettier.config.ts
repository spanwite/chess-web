import { type Config } from 'prettier';

const config: Config = {
  arrowParens: 'always',
  singleQuote: true,
  jsxSingleQuote: true,
  useTabs: false,
  tabWidth: 2,
  trailingComma: 'es5',
  semi: true,
};

export default config;
